package com.fourcamp.sbanco.domain.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.fourcamp.sbanco.domain.dto.role.AssociaRoles;
public interface AssociaRolesRepository extends JpaRepository<AssociaRoles, Long>{
}
